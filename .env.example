# App
APP_NAME=TimeTracker
APP_ENV=dev
APP_DEBUG=true
HOST=0.0.0.0
PORT=8089

# Security
UI_USERNAME=admin
UI_PASSWORD_HASH=$2b$12$use_passlib_to_generate_hash
API_TOKEN=change-me
CORS_ORIGINS=http://localhost:8089,http://127.0.0.1:8089

# DB
DB_URL=postgresql+psycopg://tracker:tracker@db:5432/tracker
# Local demo (no docker): sqlite+aiosqlite:///./dev.db
